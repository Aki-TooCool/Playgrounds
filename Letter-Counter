# Letter counter I made in Python
# Find the number of letters in a sentecne
inpt = str(input())
count = 0

inp = inpt.replace(" ", "")
# Removing the space cuz not needed
for x in inp:
    count += 1

print(count)
